name: Daily Fortune Job

on:
  schedule:
    - cron: "0 16 * * *"  # 台灣凌晨 12 點（UTC+8）

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Call Supabase Daily Fortune Function
        run: |
          curl -X POST "https://ypoikgkkikfxtgnsnocd.functions.supabase.co/daily-fortune" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE }}"
